import MenuList from './components/MenuList.vue';
import NamedSlotExam from './components/NamedSlotExam.vue';

let app = new Vue({
    el: '#pz',
    components: {
        MenuList, NamedSlotExam
    },
    data(){
        return {
            
        }
    },
    mounted(){
        
    },
    methods: {
        
    }
});