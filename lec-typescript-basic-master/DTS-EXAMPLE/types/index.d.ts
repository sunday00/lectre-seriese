export interface Example {
    alpha: string;
}
export declare class Sample implements Example {
    alpha: string;
    constructor(some: string);
    setAlpha(some: string): void;
    getAlpha(): string;
}
